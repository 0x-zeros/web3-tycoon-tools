# WireGuard VPN 脚本使用说明

## 📌 重要提示
- **推荐使用 IPv4 双栈 VPS**（同时有 IPv4 和 IPv6）
- AWS Lightsail IPv6-only 不支持（缺少 IPv6 前缀路由）

## 🚀 快速安装

### 1. 安装 WireGuard
```sh
# 切换到 root
sudo -i

# 下载安装脚本
curl -o ubuntu_wireguard_install.sh https://raw.githubusercontent.com/0x-zeros/web3-tycoon-tools/main/vps-tools/ubuntu_wireguard_install.sh

# 执行安装
chmod +x ubuntu_wireguard_install.sh
./ubuntu_wireguard_install.sh
```

**安装完成后**：
- 扫码导入客户端配置，或下载 `client1.conf`、`client2.conf` 到本地
- 在云厂商控制台放行 UDP 51820 端口

### 2. 清理客户端密钥（推荐）
客户端配置完成后，删除 VPS 上的客户端私钥：
```sh
curl -o ubuntu_wireguard_cleanup_client_keys.sh https://raw.githubusercontent.com/0x-zeros/web3-tycoon-tools/main/vps-tools/ubuntu_wireguard_cleanup_client_keys.sh

chmod +x ubuntu_wireguard_cleanup_client_keys.sh
./ubuntu_wireguard_cleanup_client_keys.sh
```

### 3. 卸载 WireGuard
```sh
curl -o ubuntu_wireguard_uninstall.sh https://raw.githubusercontent.com/0x-zeros/web3-tycoon-tools/main/vps-tools/ubuntu_wireguard_uninstall.sh

chmod +x ubuntu_wireguard_uninstall.sh
./ubuntu_wireguard_uninstall.sh
```

---

## Snell + ShadowTLS


```sh
curl -o ubuntu_surge_stls_install.sh https://raw.githubusercontent.com/0x-zeros/web3-tycoon-tools/main/vps-tools/ubuntu_surge_stls_install.sh

带参数的运行：
sudo STLS_PORT=8443 ./ubuntu_surge_stls_install.sh

停止旧的，再安装新的:
wget https://raw.githubusercontent.com/0x-zeros/web3-tycoon-tools/main/vps-tools/uninstall_snell_stls.sh

```